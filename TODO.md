# TODO

Planned Docker-style non-interactive commands:

```text
portainerctl container start <name|id>
portainerctl container stop <name|id>
portainerctl container restart <name|id>
portainerctl container rm <name|id>

portainerctl stack start <name|id>
portainerctl stack stop <name|id>
portainerctl stack rm <name|id>

portainerctl image rm <name|id>
```
